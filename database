CREATE TABLE users (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    Name VARCHAR(50),
    Email VARCHAR(50),
    Age INT,
    DateOfBirth DATE
);
INSERT INTO users (Name, Email, Age, DateOfBirth)
VALUES ('John Doe', 'johndoe@example.com', 25, '1999-05-10');
SELECT * FROM users WHERE age = '25';
